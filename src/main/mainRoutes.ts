export interface MainRoutes {
    PROFILE: string,
    LOGIN: string,
    REGISTER: string
}

export const mainRoutes: MainRoutes = {
    PROFILE: '/profile',
    LOGIN: '/login',
    REGISTER: '/register'

};
